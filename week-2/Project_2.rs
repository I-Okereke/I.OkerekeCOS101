fn main(){
	let t: f64 = 450_000.00 * 2.0;
	let m: f64 = 1_500_000.00 * 1.0;
	let h: f64 = 750_000.00 * 3.0;
	let d: f64 = 2_850_000.00 * 3.0;
	let a: f64 = 2_850_000.0;

	let total: f64 = t + m + h + d + a;

	println!("Totale revenue of the sales record is N{}", total);

	let average = total / 5.0;

	println!("Average revenue for the sales record is N{}", average);










}